{
  "name": "ecommerce-platform-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "npm --prefix frontend install",
    "build": "npm run build --prefix frontend",
    "vercel-build": "npm run build --prefix frontend"
  }
}
