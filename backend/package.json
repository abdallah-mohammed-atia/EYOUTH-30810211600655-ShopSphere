{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "description": "Backend API for the Full-Stack E-Commerce Capstone Project",
  "main": "src/server.js",
  "scripts": {
    "pretest": "prisma generate && prisma migrate deploy",
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "cross-env NODE_ENV=test jest --runInBand --coverage",
    "test:watch": "cross-env NODE_ENV=test jest --runInBand --watch",
    "seed": "node scripts/seed.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:deploy": "prisma migrate deploy",
    "generate:jwt": "node scripts/generate_jwt_secret.js"
  },
  "dependencies": {
    "@prisma/client": "^5.4.1",
    "express-rate-limit": "^6.7.0",
    "helmet": "^7.0.0",
    "serverless-http": "^3.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "pino": "^8.16.0",
    "node-fetch": "^2.6.7",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.0.1",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.0",
    "pg": "^8.12.0",
    "pg-hstore": "^2.3.4",
    "prisma": "^5.4.1"
  },
  "devDependencies": {
    "serverless": "^3.35.0",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "sqlite3": "^5.1.7",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": ["/node_modules/"],
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/server.js"
    ]
  }
}
