server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Support React Router's client-side routing by falling back to index.html
    location / {
        try_files $uri /index.html;
    }

    # Cache static assets aggressively
    location /static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
